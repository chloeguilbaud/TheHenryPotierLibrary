package guilbaud.c.thehenrypotierlibraryapp

import guilbaud.c.thehenrypotierlibraryapp.model.Book
import guilbaud.c.thehenrypotierlibraryapp.service.BookService
import org.junit.Test

import org.junit.Assert.*

class BookServiceUnitTest {

    /*@Test
    fun book_fetching() {
        BookService.fetchBooks { bookList : Array<Book> ->
            checkBookList(bookList)
        }
    }

    private fun checkBookList(bookList: Array<Book>) {
        assertEquals("Book list size", 7, bookList.size)
        assertEquals("Book content title", "Henri Potier à l'école des sorciers", bookList[0].title)
        assertEquals("Book content price", "35", bookList[0].price)
        assertEquals("Book content cover", "35", bookList[0].cover)
        assertEquals("Book content synopsis", 3, bookList[0].synopsis.size)
    }*/

}
